name: "read_file"
type: "fs"
description: "Read the contents of a file"
operation: "read"
allowed_paths:
  - "."
parameters:
  path:
    type: "string"
    description: "File path relative to cwd (e.g., './output/main.go', './package.json')"
    required: true
